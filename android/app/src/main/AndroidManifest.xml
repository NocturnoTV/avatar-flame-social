<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- REMOVED 2026-09-15: an Android App Link on the whole
                 bloxspark.app host actively broke sign-in - the moment
                 Google/Roblox redirected the system-browser tab back to
                 bloxspark.app, Android closed that tab and reopened the app
                 before the page could even render the device-code screen
                 (let alone give the person time to read/copy the code). It
                 also would not have helped anyway: the system browser and
                 the app's own WebView have completely separate storage, so
                 the session established in the browser tab is invisible to
                 the app regardless of how fast it reopens. The device-code
                 bridge (src/lib/device-login.functions.ts) is the real way
                 back into the app - don't re-add a broad App Link here. -->

            <!-- Custom-scheme fallback for the website's "Ouvrir l'app" button
                 after signup/device-code screens (see openExternal("bloxspark://open")
                 in src/routes/auth.tsx and settings.tsx) - unlike the App Link
                 above, this needs no domain verification and works the moment
                 the app is installed, but only ever brings the app to the
                 foreground (there is no path to route to from a bare
                 "bloxspark://open", the person still types the code once
                 they're looking at the app). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="bloxspark" />
            </intent-filter>
        </activity>

        <!-- Push notifications: default small-icon/color used when a
             notification doesn't specify its own (see PushNotifications
             plugin docs) - the launcher icon is a fine default here. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@mipmap/ic_launcher" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Video posting, voice messages and photo/camera uploads in the web
         app use getUserMedia()/file inputs inside the WebView, which need
         these declared even though there's no separate native camera/mic
         feature - Capacitor's own BridgeWebChromeClient already prompts
         for and grants these at runtime once they're declared here, no
         custom MainActivity code needed. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- Required on Android 13+ to actually show a notification (Capacitor's
         PushNotifications.requestPermissions() prompts for this at runtime,
         but the permission still has to be declared here). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
</manifest>
